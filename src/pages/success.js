import React from "react"
import Layout from "../components/layout"

export default function Success() {
    return(
        <Layout>
            <h1 className="text-center">Mensagem enviada com sucesso!</h1>
        </Layout>
    )
}